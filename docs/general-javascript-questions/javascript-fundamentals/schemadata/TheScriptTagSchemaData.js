const StructuredData = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: {
    '@type': 'Question',
    name: 'What is the script tag used for in JavaScript?',
    acceptedAnswer: {
      '@type': 'Answer',
      text: 'We can use the script tag to insert JS into HTML or import scripts from external sources. JavaScript may be included practically anywhere in an HTML page using the script tag. We can use the script tag to import scripts from external file sources.',
    },
  },
};

export default StructuredData;
